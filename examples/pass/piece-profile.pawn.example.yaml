schema_version: 0.5.0
piece_profile_id: piece-profile.trace-pawn.v1
agent_id: agent.trace-worker-01
piece_type: pawn
side: sente
role_name: Trace Collection Worker
role_description: Performs bounded, repeatable trace collection inside approved workflow zones.
lifecycle_state: active
capability_profile:
  allowed_actions:
  - collect_trace
  - normalize_trace
  - forward_trace
  denied_actions:
  - modify_authorization
  - approve_settlement
  - rewrite_human_axis
  tools:
  - tool.trace-collector
  - tool.trace-normalizer
  data_scopes:
  - data.workflow-events
  - data.trace-buffer
movement_profile:
  movement_policy_ref: policy.shogi-agent-legal-moves.v0.5
  max_hops_per_move: 1
  may_cross_security_zones: false
  may_enter_restricted_zones: false
authority_scope:
  authority_scope_ref: authority.trace-worker.bounded
  max_autonomy: supervised
  requires_human_review_for:
  - access_restricted_data
  - alter_trace_history
resource_budget:
  max_tokens_per_move: 12000
  max_cost_per_move: 0.25
  currency: USD
  max_duration_seconds: 300
  max_concurrent_actions: 2
human_axis_binding_ref: human-axis.wind-mission.primary
created_at: "2026-07-27T00:00:00Z"
metadata:
  canonical_symbol: pawn
promotion_profile:
  allowed_promoted_piece_types:
  - promoted-pawn
  allowed_zone_types:
  - execution
  allowed_capability_additions:
  - validate_trace_chain
  - flag_trace_anomaly
  allowed_tool_additions:
  - tool.trace-validator
  allowed_data_scope_additions:
  - data.audit-findings
  allowed_authority_scope_refs:
  - authority.trace-worker.promoted
  max_duration_seconds: 3600
  max_resource_multiplier: 2.0
  requires_human_review: true
